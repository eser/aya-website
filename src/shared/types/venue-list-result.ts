import { type ResultType } from "./result-type.ts";
import { type Profile, type ProfileList } from "./profile.ts";

type VenueListResult = ResultType<ProfileList>;

export { type VenueListResult, type Profile, type ProfileList };
