{
  "name": "shared",
  "version": "1.0.0",
  "scripts": {
    "cleanup": "rm -rf pnpm-lock.yaml yarn.lock package-lock.json node_modules/",
    "typecheck": "tsc --noEmit",
    "format:write": "deno fmt",
    "format:check": "deno fmt --check"
  },
  "devDependencies": {
    "typescript": "^5.1.6"
  }
}
